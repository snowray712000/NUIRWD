import { TextWithSnDirective } from './text-with-sn.directive';

describe('TextWithSnDirective', () => {
  it('should create an instance', () => {
    const directive = new TextWithSnDirective();
    expect(directive).toBeTruthy();
  });
});
